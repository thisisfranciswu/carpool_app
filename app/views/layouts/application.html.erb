<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title><%= @page_title + " &#171;" if @page_title %> <%= app_name %></title>
  <%= stylesheet_link_tag "reset", "compiled/layout.css", "compiled/common.css", "compiled/forms.css", "compiled/screen.css", :cache => true %>
  <%= javascript_include_tag "jquery", "application", :cache => true %>
  <%= yield :head %>
</head>

<body id="<%= @controller.controller_name %>_controller" class="<%= @template.instance_variable_get(:@_first_render).name %>">

  <div class="page_width">

    <%= render :partial => "shared/masthead" %>

    <div id="page_title">
      <% if @content_for_page_options %>
        <span class="page_options"><%= yield :page_options %></span>
      <% end %>
      <% if @page_title %>
        <h2><%= @page_title %></h2>
      <% end %>
    </div>
    <div id="content" class="clearfix <%= @layout %>">
      <% if @content_for_sidebar_left %>
        <div id="sidebar_left"><%= yield :sidebar_left %></div>
      <% end %>
      <div id="main">
        <%- flash.each do |name, message| -%>
          <%= content_tag :p, message, :class => "flash #{name}" %>
        <%- end -%>
        <%= yield %>
      </div>
      <% if @content_for_sidebar_right %>
        <div id="sidebar_right"><%= yield :sidebar_right %></div>
      <% end %>
    </div>

    <%= render :partial => "shared/footer" %>

  </div>

</body>
</html>